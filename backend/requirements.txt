# Production FastAPI backend. Direct dependencies are pinned so deployments
# and CI install the same versions.
fastapi==0.141.1
uvicorn==0.52.4
python-dotenv==1.2.2
pymongo==4.17.0
requests==2.34.2
stripe==15.5.1
pytest==9.1.1
